# RFP Use Case Extractor - dependencies
# Install with:  pip install -r requirements.txt

# --- User interface ---------------------------------------------------------
streamlit>=1.49,<2.0          # width="stretch" needs 1.49+
pandas>=2.2                   # backs Streamlit's editable review table

# --- AI orchestration -------------------------------------------------------
# langchain-core + the Google integration ARE LangChain; the umbrella
# "langchain" package is not needed for this app and would only slow installs.
langchain-core>=0.3.0,<1.0
langchain-google-genai>=2.1.0
pydantic>=2.9                 # defines the structured output schema

# --- Documents --------------------------------------------------------------
pypdf>=5.1                    # reading the RFP PDF page by page
openpyxl>=3.1                 # Excel report
python-docx>=1.1              # Word report

# --- Configuration ----------------------------------------------------------
python-dotenv>=1.0            # loads the .env file

# --- Testing ----------------------------------------------------------------
pytest>=8.3
